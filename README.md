# RR-promo

A lightweight, frontend-only promotional site for NB Dance Awards.

## Run locally

```bash
npm install
npm run dev
```

The project deliberately contains no database, payment integration, API routes, user accounts, bundled media, or server-side services.
# RR-promo
